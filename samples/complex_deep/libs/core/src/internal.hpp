#pragma once
// Private header: reached only by a file-relative include from the sources
// beside it, never through an include directory.
namespace core::internal { inline constexpr int kBase = 10; }
